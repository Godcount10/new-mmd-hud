# Dragon Raja 迁移实例

此实例来自：

`E:\MMD_work\元素周期表\mmd-hud-dragon-raja`

迁移后的主题位于 `src/hud/themes/dragon-raja/`，通过 `src/renderers/dragonRajaStageRenderer.ts` 挂载到新版 MMD SDK 的 `stage.el()` 舞台中。旧项目的 bridge 实现没有复制；`dragonRajaContext.ts` 只复用旧协议的类型，并将动作映射到新版 SDK。

## 已适配

- 发送消息：`sdk.message.send`
- 设置输入框：`sdk.input.set`
- 提交消息编辑：`sdk.message.edit`
- 退出舞台：`sdk.stage.close`
- 新版 Runtime 的消息、流式、会话和舞台事件同步为主题快照

主题中依赖旧版原生能力的面板动作会显示“新版 SDK 尚未开放此原生能力”的占位反馈，不会调用旧 bridge 或旧页面 DOM。

## 本地预览

```powershell
npm run dev:dragon-raja
```

打开 `http://127.0.0.1:5183/stage-preview.html`。

## 构建注入产物

```powershell
npm run build:dragon-raja
```

最终 JSON 位于 `dist-hud/dragon-raja/mmd-hud.json`。构建会执行类型检查、实例模块隔离检查、正则分片和 JSON 校验；当前实例不包含 Spine 或 Live2D 模块。

## AI 判读标记

龙族 HUD 不会自行理解剧情语义。AI 需要在 assistant 回复中输出约定的 `[字段=值]` 标记，HUD 再解析这些标记并更新角色档案、状态栏、论坛和行动选项。

### 角色档案

最多支持 6 名角色。角色必须提供姓名才会显示在角色列表中：

```text
[角色1姓名=路明非]
[角色1身份=卡塞尔学院学生]
[角色1关系=同行者]
[角色1血统=B级]
[角色1言灵=君焰]
[角色1装备=炼金刀]
[角色1特质=警觉]
[角色1描述=看起来十分疲惫]
[角色1台词=你终于来了。]
[角色1我方台词=我不知道这里是什么地方。]
[角色1侧写=情绪紧张但仍在观察环境]
```

支持的角色字段为：`姓名`、`身份`、`关系`、`血统`、`言灵`、`装备`、`特质`、`描述`、`台词`、`我方台词`、`侧写`。

### 时间和普通状态

```text
[时间=2009 年 11 月 3 日 23:40]
[体力=82]
[理智=65]
[当前地点=英灵殿]
[任务=调查冰窖]
```

`时间`会显示在档案区域。没有被识别为保留字段的标记会作为普通状态显示，最多保留最后 6 项。

### 行动选项

最多支持 3 个行动选项。标题用于显示，正文用于写入输入框：

```text
[选项1标题=调查现场]
[选项1=前往现场调查异常痕迹]
[选项2标题=返回大厅]
[选项2=暂时回到大厅观察情况]
```

点击选项只会把正文写入输入框，不会自动发送。如果只提供 `[选项1=文本]`，文本会同时作为标题和内容。

### 守夜人论坛

```text
[头条1=学院内部出现异常通知]
[头条2=执行部正在封锁北区]
[帖子作者=匿名用户]
[帖子正文=有人在钟楼附近看见了龙影。]
[回复1昵称=夜班学生]
[回复1=我也听到了奇怪的声音。]
```

只要存在头条、帖子正文或回复，HUD 就会显示论坛面板。

### 覆盖规则

HUD 只读取最新一条包含标记的 assistant 消息，并用它完整替换当前档案，不会把多条消息中的字段拼接起来。例如新消息不再发送某个字段时，旧值会消失，不会残留。

所有标记值按纯文本处理。AI 不能通过标记注入 HTML；角色立绘也不会直接使用 AI 提供的 URL，而是由本地角色名映射表决定。当前示例没有配置角色立绘映射时，会显示 CSS 占位图。

### 开局档案

开局页面会把玩家选择整理成一段普通文本发送给 AI，例如：

```text
我的 Dragon Raja 开局档案：姓名：未命名角色；性别：不公开；年龄：18岁；粗略阶层：普通；具体身份：普通高中生；是否已觉醒：否；血统评级：你还未觉醒，一切皆有可能；言灵：未觉醒，暂不选择。请根据这份档案开始故事。
```

之后由 AI 在剧情回复中输出上述标记。前端负责解析和展示，不会自行推断血统、关系或剧情结果。
